// let ingresso = true;

// ingresso = false;

// console.log(ingresso);

// let valor = null; //a nivel de desenvolvimento, null é utilizado para indicar que a variável não tem valor, ou seja, ela existe, mas não tem um valor atribuído.

// let valor; //a nivel de sistema, quando uma variável é declarada, mas não é inicializada, ela recebe o valor undefined, ou seja, ela existe, mas não tem um valor atribuído.

